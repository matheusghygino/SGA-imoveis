---
// src/components/ContactForm.astro
---

<form class="w-full">
    <!-- Nome -->
    <div class="mb-6">
      <label for="nome" class="block text-sm font-medium text-neutral-700 mb-2">
        Seu Nome Completo
      </label>
      <input
        type="text"
        id="nome"
        name="nome"
        required
        class="w-full px-4 py-3 rounded-lg border border-neutral-300 focus:border-secondary focus:ring-2 focus:ring-secondary/50 transition-all"
        placeholder="Como gostaria de ser chamado"
      />
    </div>
  
    <!-- Contato -->
    <div class="grid md:grid-cols-2 gap-6 mb-6">
      <!-- Telefone -->
      <div>
        <label for="telefone" class="block text-sm font-medium text-neutral-700 mb-2">
          Telefone
        </label>
        <input
          type="tel"
          id="telefone"
          name="telefone"
          required
          class="w-full px-4 py-3 rounded-lg border border-neutral-300 focus:border-secondary focus:ring-2 focus:ring-secondary/50 transition-all"
          placeholder="(XX) XXXXX-XXXX"
        />
      </div>
  
      <!-- Email -->
      <div>
        <label for="email" class="block text-sm font-medium text-neutral-700 mb-2">
          E-mail
        </label>
        <input
          type="email"
          id="email"
          name="email"
          required
          class="w-full px-4 py-3 rounded-lg border border-neutral-300 focus:border-secondary focus:ring-2 focus:ring-secondary/50 transition-all"
          placeholder="seuemail@exemplo.com"
        />
      </div>
    </div>
  
    <!-- Mensagem -->
    <div class="mb-8">
      <label for="mensagem" class="block text-sm font-medium text-neutral-700 mb-2">
        Como Podemos Ajudar?
      </label>
      <textarea
        id="mensagem"
        name="mensagem"
        rows="4"
        class="w-full px-4 py-3 rounded-lg border border-neutral-300 focus:border-secondary focus:ring-2 focus:ring-secondary/50 transition-all"
        placeholder="Fale sobre o tipo de propriedade que busca ou outra necessidade"
      ></textarea>
    </div>
  
    <!-- CTA -->
    <div class="text-center">
      <button
        type="submit"
        class="inline-flex items-center justify-center gap-3 bg-secondary hover:bg-secondary-dark text-white px-8 py-4 rounded-lg text-lg font-semibold transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl"
      >
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
        </svg>
        Enviar Mensagem
      </button>
      <p class="mt-3 text-sm text-neutral-600">
        Prometemos não compartilhar seus dados
      </p>
    </div>
  </form>